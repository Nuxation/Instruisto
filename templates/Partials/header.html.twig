<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
	{% if is_granted('ROLE_USER') %}
		<a class="navbar-brand" href="{{path('index')}}"><img src="{{ asset('assets/images/icone/Logo_v2.png') }}" alt="" height="50px"></a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse d-flex justify-content-end" id="navbarColor01">
			<ul class="navbar-nav">
				<li class="nav-item d-flex align-items-center">
					<a href="{{path('annonce_add')}}" class="btn btn-secondary mx-2 d-flex">Crée une annonce</a>
				</li>
				<li class="nav-item dropdown mr-5">
					<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
						<span class="align-items-end">{{app.user.prenom}}</span>
						<img src="
						{% if app.user.avatar is not null %}
							{#{{app.user.avatar}}#}
							{{ asset('/images_Profil/'~ image) }}
						{% else %}
							{{asset('assets/images/avatar-anonyme.gif')}}
						{% endif %} " alt="Avatar" class="rounded-circle avatar">
					</a>
					<div class="dropdown-menu">
						<a href="#" class="dropdown-item">Mes Annonces</a>
						<a href="{{ path('profil')}}" class="dropdown-item">Mon profil</a>
						<a href="#" class="dropdown-item">Mes messages</a>
						<a href="{{path('app_logout')}}" class="dropdown-item">Se déconnecter</a>
					</div>
				</li>
			</ul>
		</div>

	{% else %}
		<a class="navbar-brand" href="{{path('accueil')}}"><img src="{{ asset('assets/images/icone/Logo_v2.png') }}" alt="" height="50px"></a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse text-right" id="navbarColor01">
			<ul class="navbar-nav mr-auto"></ul>
			<div class="form-inline my-2 my-lg-0">
				<a href="{{ path('app_login') }}" class="btn btn-primary mx-2 my-2 my-sm-0">Se connecter</a>
				<a href="{{ path('app_register') }}" class="btn btn-secondary mx-2 my-2 my-sm-0">S'inscrire</a>
			</div>
		</div>
	{% endif %}
</nav>
